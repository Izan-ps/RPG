SET SCHEMA SQLIZAN;
CREATE OR REPLACE FUNCTION NOMBYDIR (DIR VARCHAR(50)) RETURNS TABLE
(
NOMBRE VARCHAR (50)
)
LANGUAGE SQL
RETURN SELECT NOMBRE FROM CLIENTES WHERE DIRECCION=DIR;

-- Ejecucion de la funcion
-- SELECT * FROM TABLE(SQLIZAN.NOMBYDIR ('DIRECCION 23'));